# 2026-09-04

## beta6 反馈收集（第一批进行中）

- 创建 `beta6-feedback-scratch.md`（仓库根），沿用 beta5 的"用户原话 → 代码调查 → 拷问 → 定稿"格式。
- **Issue 1【bug】侧栏 toggle 仍不对齐**：#66 / `0ed1129` 修过一次但没修对——删 `padding: var(--space-2)` 后按钮盒 18px，SVG 中心在 navItem 盒内偏移 9px；navGlyph 中心在 navItem 盒内偏移 19px。**两态都差 10px**。撤销删除、加回同款 padding，让按钮盒复用 navItem 38px 模型——双方视觉中心严格齐。
- **Issue 2【bug】语言切换弹层里两个 menuitem 渲染成一行（不是 CSS 设计选错，是布局 bug）**：`LanguageSwitcher.module.css:86-88` 的 `.popover` 只设了 `min-width: 120px`、没 display，`.option` 按钮默认 `inline-block`——inline-block 子元素在 block 父容器里按基线横向排列，所以两个 menuitem 被横排放成 `English | 中文` 一行两列。**owner 选 A 方案**（垂直列表 + 激活项换底色，无 ✓）= 修复后的样子。归类 bug，v1。
  - 修复 = `.popover` 加 `display: flex; flex-direction: column;`，与 `.layer` 同方向
  - 反思：owner 第一句"不是这种二选一的"是个误判——他看到的不是"二选一 toggle"，是 **inline-block 横排的 bug**。我没第一时间读出截图的红框里那是 menuitem 横排，**当成了 segmented control**。下次看到 N=2 控件横排时，**先怀疑 layout 不用怀疑设计**。
- 工具链：grill-with-docs / to-spec / to-tickets 全部在位；tracker 是 GitHub `cherishfall/misedeck`，label 走 `v1` + `ready-for-agent`。
- owner 偏好：技术细节不需要选项，**直接给视觉对比图让我自己挑**。下次涉及样式选择时优先走这个路径。
- 进行中，等下一条。

- **Issue 3【enhancement】DirectoryIndicator 工具栏 pick 按钮位置在两态间"飘"**：根因不在按钮本身，在 `.actions` 缺 `margin-left: auto`——目录模式有 `.path { flex: 1 }` 撑开，按钮在右端；全局模式无 path，按钮紧贴 eyebrow 看着"靠左"。修 = 一行 CSS（`margin-left: auto` on `.actions`）。
- **Issue 4【enhancement】全局模式工具栏 eyebrow 改"全局模式"**：拆 i18n key——`globalButton` 同时被两处用（eyebrow + 切回按钮），语义不同。拆为 `globalMode`（eyebrow，4 字）+ 保留 `globalButton`（按钮，2 字）。中间留白，**不填 "—" 占位**。
- 合并：Issue 3+4 同一 ticket（同一组件 + 连带 i18n 4 个文件改动；分开做翻译 SSR 审查不划算）。
- owner **第二次翻盘**位置决策（Q3 选 B 后改 A；这次选"最左"后改"最右"）。模式记下来：owner 看视觉候选时会用真截图和自身预期反复对，对不上就撤回。**永远把候选做得最具体（PNG-like mockup）、永远准备 "选错了怎么办" 的回路**。

- **Issue 5【enhancement】3 面板（侧栏 / 顶工具栏 / 底执行面板）背景色分层**：现状 light `--hull: #FFFFFF` 脱离 warm parchment 家族是"寡淡"根因；dark 只有 1 层。**用 mise.jdx.dev 官网实际 CSS 取色**（WebFetch 主页 → curl 拉 style.css → grep `--vp-c-bg-*`）：`bg-soft #F5EDE3 / #1C1614` 侧栏、`bg-alt #F0E6DA / #161618` 顶工具栏、`bg-mute #E8DDD0 / #261E1A` 底面板。**dark 端顶工具栏偏离官网值**（官网 `#161618` 太淡）→ 选 `#1F1816`（bg-soft 到 bg-mute 的过渡色，仍在家族）。新加 2 个 token：`--hull-soft` / `--hull-deep`；light `--hull` 从 `#FFFFFF` 改 `#F5EDE3`。
  - 关键教训：**owner 遇到"配色不满意"类问题时，第一反应应该是查"官方/上游"的调性参考**（mise.jdx.dev 是这个项目的 brand source，直接 WebFetch 拉 CSS 最快），不要凭"warm 之类"脑补候选。
  - **二次审查教训**：owner 用 `/grill-with-docs 重新审查` 时，发现 `--panel` 语义碰撞——`tokens.css:50` `--panel: var(--hull)` 被 11 个组件消费（FloatingMenu / ConfirmDialog / Banner / Panel 等），直接改 `--hull` 会让 popover 也换暖色（"浮"→"沉"），**是误伤**。**改 --hull 语义前必须 grep 所有 `var(--hull)` / `var(--panel)` 的消费方**。
  - 定稿：拆 `--panel` 语义（elevated 表面保持 `#FFFFFF` / `#1C1614` 不动），chrome 面板走 `--hull` / `--hull-soft` / `--hull-deep`。同时出一个 ADR-0006 解释。
  - **三次定稿（owner 推翻"保持原值"）**：owner 说「我提出的问题只是我能发觉的，会有遗留的，要改就都统一改吧」——elevated 表面也统一换暖色。`--panel` 新值由助手按专业判断定（owner 明确"选专业角度、不再问"）：**`#F8F0E2` / `#221C1A`**，介于 page 和 sidebar 之间的 warm 值——light 比 page 暗 2.4%、比 sidebar 亮 1.3%；dark 比 sidebar 亮 21%、比 deck 暗 14%。**shadow 撑 lift、颜色撑 visible**。不用 mise `bg-elv` 精确值（light `#FFFFFF` 中性白 off-family、dark `#202127` 蓝灰 off-brand）。**实现红利**：11 个 `--panel` 消费组件 0 行改动，自动跟变量走。
  - 关键教训：**owner 报的问题只是他能发觉的子集**——修一类问题时默认修整个类（"要改就都统一改"），不要只修被点名的实例；但"统一改"不等于"统一成一个值"，elevated 仍要和 chrome 保持可辨间隙。
- **Issue 6【enhancement】执行面板 headerRight 重排**：当前 `[复制] [状态] [×]`，owner 要 `[状态] [复制] [关闭]` 且关闭改文字按钮。调查结论：`.headerRight` 纯 flex 无定位依赖，纯 JSX 重排；`.actionBtn` 已是共用文字按钮样式（`×` 也在用），只换内容；`execution.dismiss` key 双语已存在（en "Dismiss" / zh "关闭"，当前是 aria-label）——**1 个 tsx 文件、0 CSS、0 i18n**。running 态的"取消"本来就是文字按钮，天然符合新顺序。
- **Issue 7【enhancement】执行面板不自动弹**：根源是 `useExecution.ts:94` `start` action 的 `isOpen: true`。改为 `isOpen: state.isOpen`（关着就不弹、开着就保持）。手动入口 `ExecutionPanelAffordance` 已存在（状态点带 tone、失败红点、`reopen` i18n 双语在位）。**1 个文件 ~3 行、0 i18n、0 测试影响**。不破 ADR-0005（transcript 照常记录，只是默认收起）。失败也不自动弹，若太安静后续单独 follow-up。
- **Issue 8【enhancement】全站缩窗行为统一规范（owner 授权专业定稿）**：审计全部 8 页 + 骨架——骨架层（侧栏固定宽、内容区 `min-width:0 + overflow-x:hidden`、顶栏路径 ellipsis、8 页 `.page` 同构）**已健康**，不一致只在长数据层。定稿"四层规范"：① 窗口 900×600 最小尺寸已存在保持 ② 骨架现状即标准写进规则 ③ 内容层：表格长数据唯一策略 = `table-layout: fixed` + `nowrap` + `ellipsis` + **title tooltip（硬验收）**；`break-all` 9 处清零（DirectoryPreview 3 + Settings 3 + Tasks 1 + EnvPage 2）；共享 Table 加 fixed 支持（不改默认）；散文 `break-word` 保留；日志/文件预览滚动容器为合法例外 ④ `ui-ux-rules.md:32` 从"二选一"收紧为单一默认 + 补缩窗总规则，中英镜像。
- **发布节奏**：8 条定稿攒齐后问 owner 是否发 GitHub（to-spec → to-tickets 工具链已确认在位），owner 选「先不发，继续攒」——继续收集反馈，SPEC + tickets 缓发。
- **Issue 9【enhancement】链接工具选择目录按钮统一**：ToolsPage 用 `variant="secondary"`、顶栏是 bespoke `actionPrimary`（beam 实心）。定稿：改 `variant="primary"`（1 行）；**不动顶栏**（Issue 3/4 刚验收，避免翻盘）；字形差异（mono uppercase 4px vs font-ui 6px）若 owner 仍嫌刺眼再 follow-up 收敛组件。规则：全局动作（选择目录）全站唯一视觉角色 = primary。
- **Issue 10【enhancement】placeholder 截断 + 操作列 variant 语义统一**：a) `.inputName { width: 14ch }` 装不下 10 全角字（≈20ch）→ 改 20ch，添加表单和行编辑器同 class 两处生效，0 i18n；b) 全站操作列 variant 映射定稿：primary=行主行动（运行/安装/添加/保存）、secondary=常规（切换/编辑/选择目录）、danger=破坏性（卸载/移除）、ghost=放弃/低频（取消/在编辑器打开）。改动：EnvPage 编辑→secondary、移除→danger（2 行）+ TasksPage 编辑→secondary（1 行）+ 映射表写进 ui-ux-rules.md 中英镜像。ToolsPage 切换 secondary/卸载 danger 本来就是正确范式。
- **beta6 发布完成**：SPEC 父 **#74** + tickets **#75-#82**（8 个，全部无阻塞、可立即开工，label v1 + ready-for-agent；#75/#76 是 bug，其余 enhancement）。映射：#75←Issue1 toggle、#76←Issue2 语言切换、#77←Issue3+4 工具栏、#78←Issue5 配色+ADR、#79←Issue6 头部重排、#80←Issue7 不自动弹、#81←Issue8 缩窗统一、#82←Issue9+10 按钮语义+placeholder。拆分和测试缝 owner 已确认；测试缝 = npm run ci + 运行目验（经济验证），无新增测试层。
- **发布后增补（owner 提醒后拍板）**：assistant 主动提醒 Out of scope 两项 → owner 全做 + 授权"主动发现问题"。① **#80 已改**：失败时面板关着 → 自动弹一次（唯一例外）；成功/取消仍只亮 affordance。② **新开 #83**：顶栏 pick 按钮 bespoke 样式删除、收敛到共享 Button primary（mono→UI 字体、4px→6px，owner 明确批准该区域视觉变化）。③ **#74 SPEC 已同步**（user stories 19/20、implementation decisions、Out of scope 两项移入 scope 并注明）。
- **扫查结论（已汇报 owner）**：placeholder 截断全站只有 EnvPage 一处（Settings 14ch/18ch、Tools/Tasks 输入宽度都装得下各自 placeholder）——#82 已覆盖；另一个 `×` 在 DirectoryIndicator 最近列表删除钮，是带 aria-label 的 IconButton 图标按钮，合法；ExecutionPanel 的 mono `.actionBtn` 保持 mono（终端语境有意为之），已写入 #74 Out of scope 防后续误收敛。
- **文档沉淀（owner 拍板"全部按推荐"）**：commit `0d892fb`（push 首次因网络 502 失败，稍后重试成功，本地与远端一致）——① `ui-ux-rules.md` 中英镜像：长数据唯一默认收紧（fixed+ellipsis+title，hover 完整值硬要求）、新增「缩窗行为」「操作按钮」（variant 映射表 + 全局动作唯一角色 + mono 例外）「执行面板」（不自动弹 + 失败弹一次唯一例外 + 头部顺序）三节；② `AGENTS.md` 中英镜像工作约定加"反馈评审以主动扫查收尾（含查过没问题的项也要汇报）"。#81/#82 验收清单改为"核对已合入规则，勿重写文档"。
- **扫查自纠错**：上轮我说"env remove 不需要确认弹窗"是**错的**——`ui-ux-rules.md:25` 一直绑定 "uninstall, unset, overwrite always confirm"，ConfirmDialog 只有 ToolsPage 卸载在用（`:694`），EnvPage remove 直接 `onWrite` 违反现行规则。已开 **#84**（bug）：env remove 走 ConfirmDialog + 展示 `mise unset <KEY>` 教学命令。教训：给 owner 的扫查结论必须先对文档现状核实，不能凭"CLI 惯例"推理推翻成文规则。
- beta6 最终 ticket 面：**#75-#84 共 10 个**（#75/#76/#84 bug，其余 enhancement），SPEC 父 #74（已同步 scope）。

---

## beta6 实现会话（同日下午，自主推进，因 429 限额中断）

owner 指令：读 handoff 梳理未完成 issue，用 implement 技能逐个 subagent 推进；
**禁止视觉验证/仿真验证**（owner 自己手动验）、禁止过度测试；每完成一个立即 commit + push；
每 issue 单独 subagent 控上下文；遇限额先更新 handoff 再停。

### 已完成并关闭（6 个，master，`npm run ci` 绿）

| Ticket | 内容 | Commit |
|---|---|---|
| #75 | `.collapseToggle` 恢复 `padding: var(--space-2)` 复用 navItem 38px box（撤销 `0ed1129`），重写 "18px box" 注释 | `b603dd0` |
| #76 | `.popover` 加 `display:flex; flex-direction:column`（原 block 父 + inline-block 子 → 横排） | `37dea50` |
| #77 | `.actions` 加 `margin-left:auto`；新增 i18n `directory.globalMode`（GLOBAL MODE / 全局模式），从 `globalButton` 拆出 | `a611141` |
| #78 | `--panel` 拆 chrome/elevated 两角色；新增 `--hull-soft`/`--hull-deep`；ADR-0006 中英双写 | `a49d453` |
| #79 | header 重排 状态→复制→关闭；三处 `×` → `execution.dismiss` 文字（复用 `.actionBtn`，0 CSS / 0 i18n） | `cd5a710` |
| #80 | `start` 的 `isOpen: true` → `state.isOpen`；失败且面板关时在 **failed 转换处**开一次 | `6ed1faa` |

### 未完成：#81（半途）/ #82 / #83 / #84

**#81 半成品已停在 `wip/81-long-data` 分支（`a5ca1b1`，已推远端），master 干净且绿。**
subagent 被限额打断前已改 14 文件（`Table.tsx`+css、ActivationBanner/DataRow/StyleGuide css、
DirectoryPreview/EnvPage/SettingsPage/TasksPage/ToolsPage 的 tsx+css）。
**未验证**：ci 是否过、`break-all` 是否清零、ellipsis 是否都带 `title`、两个 `ui-ux-rules.md`
是否改了（大概率没改，agent 死在半途）。恢复时先 `git diff 6ed1faa..wip/81-long-data` 审一遍，别直接信。

### 本会话的坑（下次直接避开）

- **HANDOFF.md 会过期**：它写着 "Nothing left to implement"，实际 beta6 批 10 个 ticket 全没做。
  **判断 frontier 一律以 `gh issue list --state open` 为准，handoff 只当历史。**
- **scratch 文件也会过期**：`beta6-feedback-scratch.md` 的 Issue 7 定稿"失败也不自动弹"，
  但 owner 后来翻盘，#80 标题变成 "auto-open once on failure"。**GitHub issue body > scratch 文件。**
- 每次 `git push` 都 502 / HTTP2 framing，必须 `git -c http.proxy=http://127.0.0.1:7890 push`。
- subagent 被限额打断时**工作区会留下未提交半成品** → 先 commit 到 WIP 分支保住，别弄脏 master。
- #80 "once" 的正确实现位：**在 failed 状态转换处**（不是 `start`）。单次 run 只发一次 failed
  转换（`exit` 非零 + 至多一个 `fail`），用户 dismiss 后不会有后续事件再弹；下一次失败 run 再弹一次是预期。

### 恢复顺序（有依赖，别乱序）

**#81 → #82 → #83 → #84**。#81 不能碰 EnvPage/TasksPage 的 button `variant`（归 #82）；
#83 要改的 `DirectoryIndicator` 已被 #77/#78 改过，放后面。

已更新 HANDOFF.md 并把 `beta6-feedback-scratch.md` 纳入版本管理（commit `5359464`）。

---

## beta6 实现完成（同日 16:00–16:30 续推，主会话执行）

限额恢复后继续。**#81–#84 全部完成关闭，beta6 批 10/10 done。**

| Ticket | 内容 | Commit |
|---|---|---|
| #81 | Table 加**opt-in** `fixed` prop（默认不变）；5 张表启用；`break-all` **12→0**（旧审计漏了 ActivationBanner/DataRow/StyleGuide）；所有 ellipsis 单元格补 `title`。从 `wip/81-long-data` 救回并审计，代码是好的；规则文档层 `0d892fb` 已合入，核实未重写 | `dc3cdc6` |
| #82 | EnvPage 编辑 ghost→secondary、移除 ghost→danger；TasksPage 编辑 ghost→secondary；链接表单选目录 secondary→primary；`.inputName` 14ch→20ch。映射表已在 ui-ux-rules，核实未重写；0 i18n | `dbb9d26` |
| #83 | 两个 pick 按钮收敛到 `<Button variant="primary" size="sm">`，`.actionPrimary` 删除（全站 0 残留）。owner 批准的视觉变化：mono uppercase/4px → UI font 600/6px | `a76327c` |
| #84 | env remove 走共享 ConfirmDialog，展示确切 `mise unset <KEY>`（全局态显示 `-g`）；新增 `env.confirm.remove.{title,body}`（409 keys）。**连带修同类 bug**：`tools.confirm.uninstall.title` 用了单花括号 `{tool}`，i18next 默认 `{{}}` 根本不插值——卸载弹窗一直在显示字面 "{tool}"，已改 `{{tool}}` | `6968c14` |

handoff 终态 `f7f8c38`。剩余开放：**#74 + #45/#61/#65**，全等 owner 视觉验收。
`wip/81-long-data` 已成历史（内容进了 `dc3cdc6`），按 AGENTS.md 不删分支。

### 本会话踩到的两个工程坑（重要）

1. **对同一文件并行发多个 Edit 会静默丢改动**——报"成功"但实际没写入（#83 丢 1 个、#84 连丢 3 个），
   直到 `npm run ci` 报 unused import / undefined 才暴露。**同一文件的多次编辑必须逐个串行发**，
   且每次编辑后用 CI/grep 核实。
2. **i18next 插值分隔符默认是 `{{ }}`**：项目里 `env.tooltip` 用 `{{tool}}`（对）而 `tools.confirm.uninstall`
   用 `{tool}`（错、不插值）。写新 i18n key 前先查 `i18n/index.ts` 的 interpolation 配置，别照抄邻居。
   单花括号 bug 已在 #84 一并修掉。

### 判断记录（已在 #84 关闭评论里报备 owner）

- rename 路径（onSave 里 unset 旧 key + set 新 key）**没**加确认弹窗——它是用户已点 Save 的显式动作，
  不是独立破坏性按钮。owner 若不同意可翻盘。
- `{tool}` 插值 bug 严格说超出 #84 范围，按"修一类问题"惯例连带修掉并**显式报备**而非埋没。

### owner 拍板（2026-09-04 16:54）

「两个需要你知道的发现，按照你认为对的来」——两个判断均按原实现定案：
① `{tool}`→`{{tool}}` 修复留在 #84 内，不回溯拆票；② rename 路径不加确认弹窗。
已在 [#84 评论](https://github.com/cherishfall/misedeck/issues/84#issuecomment-5538111495) 落档。

### 坑的"修法"沉淀

并行 Edit 静默丢改动的坑：工具行为本身改不了，已固化为用户级 skill
`~/.workbuddy/skills/same-file-edit-safety/SKILL.md`（同文件编辑串行 + 批量后 CI/grep 核实）。

---

## #85 i18n 插值护栏（同日 17:00–17:15，owner 授权非沙箱跑 npm）

owner 拷问"你说有问题就修嘛，哪怕新提 issue 和 ticket"→ 全类审计 + 开票实现：

- **全类审计结论**：en/zh 单花括号残留 0 处；490 无参 + 17 带参调用点逐一核实，脚本初报 22 个嫌疑全是
  误报（多行传参、对象简写 `{ tool, version }`、URL 里的 `https:` 干扰正则）。存量真 bug 仅 #84 已修的那处。
- **#85 已实现并关闭**（commit `1fd83ba`）：`check-i18n.ts` 新增第三个检查——TS AST 解析 keys.ts
  （要解包 `as const`，裸查 ObjectLiteral 会扑空）+ 全部 `t(I18N_KEYS…)` 调用点，双语比对占位符与传参；
  动态调用点跳过（护栏必须零误报）。490 个调用点，0 跳过，0 误报。
- **护栏首跑就抓到真 bug**：`execution.copyHint` 在 keys.ts 定义、复制按钮 tooltip 在用，
  但 en/zh 资源文件里**不存在**——i18next 回退渲染字面 key。旧 parity 只比对两个 JSON、
  从不核对 keys.ts（410 vs 409，差的就是它）。已补双语文案。
- **fixture 验证**：`{{url}}`→`{url}` 后 lint FAILED 并点名两处调用点（StyleGuide:332 / HomePage:131），还原后绿。
- **环境坑**：node_modules 丢了 `@tauri-apps/api`（非我改动所致），`npm install` 恢复；
  本地 npm 版本对 package-lock 的 churn（剥 libc 字段）已还原、不入提交。
- **新坑记一笔**：fixture 用 `git checkout -- en.json` 还原时，**把同文件里的 copyHint 修复也一起回退了**
  （fixture 和修复合在同一文件）。教训：`git checkout -- <file>` 是整文件粒度回退，
  同文件里混有"要保留的改动"时别用它，改用精准 Edit 反向修复；checkout 后必须 `git status` + 重跑检查确认。

master 最新 `1fd83ba`，CI 全绿。开放 issue：#74 + #45/#61/#65（等视觉验收）。

---

## v1.0.0-beta.7 发布（同日 17:20）

owner 拍板发版。流程全按 handoff：6 处版本号（5 文件，grep 核实 0 残留）→
`chore(release): bump` 提交 → annotated tag `v1.0.0-beta.7`（tag message 即 Release body，
中英双语，沿用 beta6 格式）→ push master + tag → release.yml 三平台构建 + Pre-release。
发版前 cargo check 绿（2 个既有 warning）。
- **beta7 内容**：beta6 反馈批全部十项（#75–#84）+ #85 护栏 + `1feb11d` 流式截断修复
  （beta.6 产物里仍带 bug，此版修掉）。
- **坑**：`-q` 静默 push tag 失败时看起来像成功——**必须 `git ls-remote` 验证 tag 落远端**。
- **坑**：push 新 commit 会**取消**在途的 CI run（GitHub 显示 X/cancelled），别当成真失败；
  `gh run list --json conclusion` 里是 "cancelled" 不是 "failure"。
- node_modules 丢 `@tauri-apps/api` 的环境问题用 `npm install` 修复（非沙箱跑，需 owner 批准）。
- **发布完成确认（17:34）**：release run 33857942721 绿，v1.0.0-beta.7 Pre-release 已发布，
  六件产物齐全（dmg/exe/deb/rpm/AppImage/SHA256SUMS），handoff 补交 `a712378`。
  唯一注解仍是无害的 Node.js 20 弃用提醒（actions v4，将来可升级）。
  **beta7 从此包含**：#75–#85 + `1feb11d` 流式截断修复。等 owner 在 beta7 上做视觉验收后关 #74 + #45/#61/#65。
